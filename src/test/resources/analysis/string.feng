
export
enum CharEncoding {
    ASCII,
    UTF16,
    UTF32,
}

class String {
    const value [*]uint8;
    const cc CharEncoding;

    func value() [*]uint8 {
        return value;
    }

    func char(index int) int32 {
        if (CharEncoding.ASCII == cc) {
            const s [&]uint8 = value;
            if (s == nil) return 0;
            return int32(s[index]);
        }
        const s [&]uint16 = value;
        if (s == nil) return 0;
        return int32(s[index]);
    }

    func len() int {
        var v = value;
        if (v == nil) return 0;
        return v.length << cc.id;
    }

    func isEmpty() bool {
        var v = value;
        if (v == nil) return true;
        return v.length == 0;
    }

}
