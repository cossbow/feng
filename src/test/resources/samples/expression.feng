func expression(a , b,c int) {
    return a +b *c;
}


func success(t *Object) Result {
    return new(Result, {
        code= 0,
        data= t
    });
}

func assignDeclare(t *Object) {
    var l List;
    var a,b List = 1,2 ;
    const A,B =  a.id , b[1+2];
    a = a.id;
    a.user.id = a.id;
    a.list[0] = {e:t};
    list[0] = [a];
}

func assignments()  {
    a,b.id,c[1] = t, t*2, t*3;
    return t,t+1,t+2;
}

func newThings() {
    var a = new(Result, {code=0});
    var b = new([4]int, [1,2]);
    var c= new( [4]int, true);
}

func expr() {
    a= 1+2-3 ;
    a= 1+ (2) -3;
    a= 1+2*3;
    a= (1+2)*+-3;
    a= 1*3^5*4;
    a= 2*4^-2*5;
    a= 1&!1;
    a= 1 | 2 ~ 3 & 4;
    a= 1 > 2 || 2 < 3 && 3 == 4;
    a= 0 <= 3<<---2+1;
    a= -a^b^-c^d;
}

func idName() {
    a= pi;
    a= math.pi;
    a= getMath().pi;
}

func call() {
    pi();
    pow(x,y);
    memset(a,0,16);
    math.pi();
    math.pow(x,y);
    memory.set(a,0,16);
    math().mod(x, y);
}

func lambda() {
    var sq func(int) int = func(v int) int {
        return v*v;
    };
}

func array() {
    var a=[[[]]][0];
}
