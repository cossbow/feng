
enum E{Stop, GO=100, Wait,}

func test(i int) {
    const e1 E = E.Stop;
    const e2 E = E[i];
    const id int = E.Stop.id;
    const v int = E.GO.value;
    const n [&#]byte= E.Stop.name;
    switch(e2) {
        case Stop {}
        case GO {}
        default {}
    }
}

func main(args [&#][*?#]byte) {
    test(1);
}
