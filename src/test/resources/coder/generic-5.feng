import std$container c;

func intHash(n &#c$Node`int,int`) int {
    return n.key;
}
func intEq(a,b &#c$Node`int,int`) bool {
    return a.key == b.key;
}
func use`T`(f func(T)) {}
func fff(i int) {}
func main(args [&#][*?#]byte) {
    var hashing = c$Hashing`int,int` {
        hash=intHash, equal=intEq
    };
    var map = c$newHashmap(hashing);
    use(fff);
}
