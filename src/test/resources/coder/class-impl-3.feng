
interface I{
    run();
}
interface J{I;}

class A (J) {
    var id int;
    func run() {
    }
}

class B:A{}

func get() *Object {
    return new(B);
}

func test(n int) {
    {
        var o *Object = new(B);
        var a = o?(*?A);
        if (a!=nil) a.run();
        var b = o?(*?B);
        if(b!=nil) b.run();
        {
            var a = b?(*?A);
            if (a!=nil) a.run();
        }
        var i = o?(*?I);
        if (i!=nil) i.run();
        var j = o?(*?J);
        if (j!=nil) j.run();
        {
            const o1 &Object = o;
            const a = o1?(&?A);
            if (a!=nil) a.run();
            const b = o1?(&?B);
            if (b!=nil) b.run();
            {
                const a = b?(&?A);
                if (a!=nil) a.run();
            }
            const i = o1?(&?I);
            if (i!=nil) i.run();
            const j = o1?(&?J);
            if (j!=nil) j.run();
        }
    }
    {
        var o *Object = get();
        var a = get()?(*?A);
        if (a!=nil) a.run();
        var b = get()?(*?B);
        if (b!=nil) b.run();
        var i = get()?(*?I);
        if (i!=nil) i.run();
        var j = get()?(*?J);
        if (j!=nil) j.run();
    }

}


func main(args [&#][*?#]byte) {
    test(1);
}
