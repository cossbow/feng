import std$bytes;
import std$string;

func test(n int) {
    var b bytes$BufferWriter;
    var s = string$fromBytes("feng");
    format(b, "Hello {}!\n", s);
    var r = b.copy();
}

func main(args[&!#][*!#]byte) {
    test(15);
}
