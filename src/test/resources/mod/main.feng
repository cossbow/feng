import test$aaa a;
import test$aab v;
import test$aac r;
import test$aad;
import std$map m;


func defaultHash`T`(k T) T {
    return (k);
}

func testHashmap(n int) {
    var map = aad$newHashmap`int,int`(defaultHash`int`);
    for(var i=0;i<n;i+=1) {
        map.set(i, i+100);
    }
    if (var r=map.get(-1); r.1){}
    if (var r=map.get(0); r.1){}
    if (var r=map.get(1); r.1){}
    if (var r=map.get(99); r.1){}
    if (var r=map.get(100); r.1){}
    if (var r=map.get(101); r.1){}
}

func intHashing(n &m$Node`int,int`) int {
    return n.key;
}

func testMap(n int) {
    var map = m$newHashmap`int,int`(intHashing);
    for(var i=0;i<n;i+=1) {
        map.set(i, i+100);
    }
    if (var r=map.get(-1); r.ok){}
    if (var r=map.get(0); r.ok){}
    if (var r=map.get(1); r.ok){}
    if (var r=map.get(99); r.ok){}
    if (var r=map.get(100); r.ok){}
    if (var r=map.get(101); r.ok){}
}


func foo(a,b v$Vector) v$Vector {
    return a.sub(b);
}

func test(n int) {
    r$use2(foo);

    a$wrap`int`(123);
    a$make(n);
}

func main(args [&!#][*!#]byte) {
    test(5);
}
