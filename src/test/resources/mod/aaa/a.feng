
export
class Complex final {
    export
    var real,imag float;

    export
    func neg() Complex {
        return {
            real = -real,
            imag = -imag
        };
    }

    export
    func add(c &!#Complex) Complex {
        return {
            real = real + c.real,
            imag = imag + c.imag
        };
    }

    export
    func sub(c &!#Complex) Complex {
        return {
            real = real - c.real,
            imag = imag - c.imag
        };
    }

    export
    func mul(c &!#Complex) Complex {
        return {
            real = real*c.real - imag*c.imag,
            imag = real*c.imag + imag*c.real
        };
    }

    export
    func div(c &!#Complex) Complex {
        var d = c.real*c.real + c.imag* c.imag;
        return {
            real = (real*c.real + imag*c.imag) / d,
            imag = (imag*c.real - real*c.imag) / d
        };
    }

}

export const ComplexZero Complex = {};
