import std$testing;
import std$sort;

func cmpInt(a, b int) int {
    if (a < b) { return -1; }
    if (a > b) { return 1; }
    return 0;
}

@Test
func testIsSortedOnly() {
    var arr [5]int = [1, 2, 3, 4, 5];
    testing$assertTrue(sort$isSorted`int`(arr, cmpInt));
}
