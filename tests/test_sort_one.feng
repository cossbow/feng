import std$testing;
import std$sort;

func cmpInt(a, b int) int {
    if (a < b) { return -1; }
    if (a > b) { return 1; }
    return 0;
}

@Test
func testOneSort() {
    var arr [3]int = [3, 1, 2];
    sort$sort`int`(arr, cmpInt);
    testing$assertTrue(arr[0] == 1);
}
