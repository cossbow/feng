import std$testing;
import std$math;

@Test
func testConstants() {
    // Test basic pure-Feng functions first
    testing$assertTrue(math$abs(5.0) == 5.0);
    testing$assertTrue(math$min(2.0, 3.0) == 2.0);
}
